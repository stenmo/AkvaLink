// SPDX-License-Identifier: Apache-2.0
//
// Single source of truth for the app version shown in the UI. Keep this in
// lock-step with pubspec.yaml `version:` and the repo-root version.txt — the
// copilot-instructions "keep app & web in sync" rule covers this.
const String kAppVersion = '0.3.1';
